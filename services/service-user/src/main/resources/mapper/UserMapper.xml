<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hao.user.mapper.UserMapper">
    <insert id="save">
        insert into user (number, password, role, create_time, update_time, phone)
        VALUES (#{number}, #{password}, #{role}, #{createTime}, #{updateTime}, #{phone})
    </insert>
</mapper>