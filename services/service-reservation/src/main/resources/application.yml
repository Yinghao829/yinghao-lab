spring:
  application:
    name: service-reservation
  datasource:
    url: jdbc:mysql://localhost:3306/lab_reservation_db?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: hao@0829
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        import-check:
          enabled: false
server:
  port: 8080